$(document).ready(function () {
    $(".about__slider").slick({
        arrow: true,
        slidesToShow: 1,
        centerPadding: '0px',
        autoplay: true,
    });
});


$(document).ready(function () {
    $(".ukrainian-heroes__slider").slick({
        arrow: true,
        slidesToShow: 3,
        // centerPadding: '0px',
        // centerMode: true,
        // adaptiveHeight: true,
        // autoplay: true,
        // variableWidth: true
    });
});

$(document).ready(function () {
    $(".partners__slider").slick({
        arrow: true,
        slidesToShow: 4,
        // centerPadding: '0px',
        // autoplay: true,
    });
});